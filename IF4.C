#include<stdio.h>
#include<conio.h>
void main()
{
int i,n;
clrscr();
	 printf("enter the value of:");
	 scanf("%d",&n);
	 printf("you have enter the value of n is %d",n);

	 i=1;
	 while(i<=n)
	 {
	 printf("\n%d",i);
	 r++;
	 }

getch();
}